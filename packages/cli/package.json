{
  "name": "@argus/cli",
  "version": "0.1.0",
  "description": "Argus CLI — cloud verification: smoke, flows, audit, tunnels, and harness wiring.",
  "type": "module",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/everyai-com/argus",
    "directory": "packages/cli"
  },
  "homepage": "https://github.com/everyai-com/argus",
  "keywords": ["mcp", "testing", "verification", "browser", "cloudflare", "cli"],
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "argus": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --format=esm --outfile=dist/index.js --banner:js=\"#!/usr/bin/env node\" && chmod +x dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@argus/shared": "workspace:*",
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.12",
    "typescript": "^5.7.0"
  }
}
