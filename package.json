{
  "name": "argus",
  "private": true,
  "version": "0.1.0",
  "description": "Argus — cloud verification platform: parallel browser testing on Cloudflare with agent-driven exploration and visual feedback",
  "packageManager": "pnpm@11.3.0",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm --filter @argus/cli build && pnpm --filter @argus/mcp build && vitest run",
    "test:watch": "vitest",
    "test:e2e:live": "node scripts/live-e2e.mjs",
    "check": "pnpm typecheck && pnpm test && pnpm build",
    "deploy": "pnpm --filter @argus/cloud deploy"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "vitest": "^4.1.11"
  }
}
